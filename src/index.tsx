import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './app/App';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

//TODO presentation: создать, сохранить, открыть, переименовать

//TODO slide: удалить, удалить несколько слайдов, выбрать, переместить, переместить несколько слайдов, изменить размер, изменить фон

//TODO block: копировать, вставить, удалить